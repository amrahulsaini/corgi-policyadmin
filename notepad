gcloud compute ssh corgi --project=work1-482817 --zone=us-central1-a

For deleting any records :

in vm :
 sudo -u corgiapp bash -c 'set -a; . /srv/corgi/.env; set +a; psql "$DATABASE_URL"' 
then :
delete from journal_entries;
update journal_lines set amount_minor = 1;
delete from premium_charges;
delete from kyb_events;
